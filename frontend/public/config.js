/**
 * 运行时配置 — 部署后直接修改此文件，无需重新构建
 *
 * 留空则使用构建时注入的默认值（由 VITE_API_BASE_URL 决定）。
 * 修改后刷新浏览器即可生效（建议清理缓存）。
 *
 * 示例：
 *   window.__APP_CONFIG__ = {
 *     API_BASE_URL: 'https://api.example.com/api',
 *   }
 */
window.__APP_CONFIG__ = {
  API_BASE_URL: '',
}
